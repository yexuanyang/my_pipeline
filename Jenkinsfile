pipeline {
    agent any
    stages{
        stage('Build') {
            steps {
                sh ''' #!/bin/bash
		            ls -la
                    echo "webhook is working!"
                '''
            }
        }
    }
}
